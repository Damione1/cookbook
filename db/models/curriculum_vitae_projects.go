// Code generated by SQLBoiler 4.14.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// CurriculumVitaeProject is an object representing the database table.
type CurriculumVitaeProject struct {
	ID                int       `boil:"id" json:"id" toml:"id" yaml:"id"`
	CurriculumVitaeID int       `boil:"curriculum_vitae_id" json:"curriculum_vitae_id" toml:"curriculum_vitae_id" yaml:"curriculum_vitae_id"`
	ProjectID         int       `boil:"project_id" json:"project_id" toml:"project_id" yaml:"project_id"`
	CreatedAt         null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`

	R *curriculumVitaeProjectR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L curriculumVitaeProjectL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CurriculumVitaeProjectColumns = struct {
	ID                string
	CurriculumVitaeID string
	ProjectID         string
	CreatedAt         string
}{
	ID:                "id",
	CurriculumVitaeID: "curriculum_vitae_id",
	ProjectID:         "project_id",
	CreatedAt:         "created_at",
}

var CurriculumVitaeProjectTableColumns = struct {
	ID                string
	CurriculumVitaeID string
	ProjectID         string
	CreatedAt         string
}{
	ID:                "curriculum_vitae_projects.id",
	CurriculumVitaeID: "curriculum_vitae_projects.curriculum_vitae_id",
	ProjectID:         "curriculum_vitae_projects.project_id",
	CreatedAt:         "curriculum_vitae_projects.created_at",
}

// Generated where

var CurriculumVitaeProjectWhere = struct {
	ID                whereHelperint
	CurriculumVitaeID whereHelperint
	ProjectID         whereHelperint
	CreatedAt         whereHelpernull_Time
}{
	ID:                whereHelperint{field: "\"curriculum_vitae_projects\".\"id\""},
	CurriculumVitaeID: whereHelperint{field: "\"curriculum_vitae_projects\".\"curriculum_vitae_id\""},
	ProjectID:         whereHelperint{field: "\"curriculum_vitae_projects\".\"project_id\""},
	CreatedAt:         whereHelpernull_Time{field: "\"curriculum_vitae_projects\".\"created_at\""},
}

// CurriculumVitaeProjectRels is where relationship names are stored.
var CurriculumVitaeProjectRels = struct {
	CurriculumVitae string
	Project         string
}{
	CurriculumVitae: "CurriculumVitae",
	Project:         "Project",
}

// curriculumVitaeProjectR is where relationships are stored.
type curriculumVitaeProjectR struct {
	CurriculumVitae *CurriculumVitae `boil:"CurriculumVitae" json:"CurriculumVitae" toml:"CurriculumVitae" yaml:"CurriculumVitae"`
	Project         *Project         `boil:"Project" json:"Project" toml:"Project" yaml:"Project"`
}

// NewStruct creates a new relationship struct
func (*curriculumVitaeProjectR) NewStruct() *curriculumVitaeProjectR {
	return &curriculumVitaeProjectR{}
}

func (r *curriculumVitaeProjectR) GetCurriculumVitae() *CurriculumVitae {
	if r == nil {
		return nil
	}
	return r.CurriculumVitae
}

func (r *curriculumVitaeProjectR) GetProject() *Project {
	if r == nil {
		return nil
	}
	return r.Project
}

// curriculumVitaeProjectL is where Load methods for each relationship are stored.
type curriculumVitaeProjectL struct{}

var (
	curriculumVitaeProjectAllColumns            = []string{"id", "curriculum_vitae_id", "project_id", "created_at"}
	curriculumVitaeProjectColumnsWithoutDefault = []string{"curriculum_vitae_id", "project_id"}
	curriculumVitaeProjectColumnsWithDefault    = []string{"id", "created_at"}
	curriculumVitaeProjectPrimaryKeyColumns     = []string{"id"}
	curriculumVitaeProjectGeneratedColumns      = []string{}
)

type (
	// CurriculumVitaeProjectSlice is an alias for a slice of pointers to CurriculumVitaeProject.
	// This should almost always be used instead of []CurriculumVitaeProject.
	CurriculumVitaeProjectSlice []*CurriculumVitaeProject
	// CurriculumVitaeProjectHook is the signature for custom CurriculumVitaeProject hook methods
	CurriculumVitaeProjectHook func(context.Context, boil.ContextExecutor, *CurriculumVitaeProject) error

	curriculumVitaeProjectQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	curriculumVitaeProjectType                 = reflect.TypeOf(&CurriculumVitaeProject{})
	curriculumVitaeProjectMapping              = queries.MakeStructMapping(curriculumVitaeProjectType)
	curriculumVitaeProjectPrimaryKeyMapping, _ = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, curriculumVitaeProjectPrimaryKeyColumns)
	curriculumVitaeProjectInsertCacheMut       sync.RWMutex
	curriculumVitaeProjectInsertCache          = make(map[string]insertCache)
	curriculumVitaeProjectUpdateCacheMut       sync.RWMutex
	curriculumVitaeProjectUpdateCache          = make(map[string]updateCache)
	curriculumVitaeProjectUpsertCacheMut       sync.RWMutex
	curriculumVitaeProjectUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var curriculumVitaeProjectAfterSelectHooks []CurriculumVitaeProjectHook

var curriculumVitaeProjectBeforeInsertHooks []CurriculumVitaeProjectHook
var curriculumVitaeProjectAfterInsertHooks []CurriculumVitaeProjectHook

var curriculumVitaeProjectBeforeUpdateHooks []CurriculumVitaeProjectHook
var curriculumVitaeProjectAfterUpdateHooks []CurriculumVitaeProjectHook

var curriculumVitaeProjectBeforeDeleteHooks []CurriculumVitaeProjectHook
var curriculumVitaeProjectAfterDeleteHooks []CurriculumVitaeProjectHook

var curriculumVitaeProjectBeforeUpsertHooks []CurriculumVitaeProjectHook
var curriculumVitaeProjectAfterUpsertHooks []CurriculumVitaeProjectHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CurriculumVitaeProject) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CurriculumVitaeProject) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CurriculumVitaeProject) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CurriculumVitaeProject) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CurriculumVitaeProject) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CurriculumVitaeProject) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CurriculumVitaeProject) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CurriculumVitaeProject) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CurriculumVitaeProject) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range curriculumVitaeProjectAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCurriculumVitaeProjectHook registers your hook function for all future operations.
func AddCurriculumVitaeProjectHook(hookPoint boil.HookPoint, curriculumVitaeProjectHook CurriculumVitaeProjectHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		curriculumVitaeProjectAfterSelectHooks = append(curriculumVitaeProjectAfterSelectHooks, curriculumVitaeProjectHook)
	case boil.BeforeInsertHook:
		curriculumVitaeProjectBeforeInsertHooks = append(curriculumVitaeProjectBeforeInsertHooks, curriculumVitaeProjectHook)
	case boil.AfterInsertHook:
		curriculumVitaeProjectAfterInsertHooks = append(curriculumVitaeProjectAfterInsertHooks, curriculumVitaeProjectHook)
	case boil.BeforeUpdateHook:
		curriculumVitaeProjectBeforeUpdateHooks = append(curriculumVitaeProjectBeforeUpdateHooks, curriculumVitaeProjectHook)
	case boil.AfterUpdateHook:
		curriculumVitaeProjectAfterUpdateHooks = append(curriculumVitaeProjectAfterUpdateHooks, curriculumVitaeProjectHook)
	case boil.BeforeDeleteHook:
		curriculumVitaeProjectBeforeDeleteHooks = append(curriculumVitaeProjectBeforeDeleteHooks, curriculumVitaeProjectHook)
	case boil.AfterDeleteHook:
		curriculumVitaeProjectAfterDeleteHooks = append(curriculumVitaeProjectAfterDeleteHooks, curriculumVitaeProjectHook)
	case boil.BeforeUpsertHook:
		curriculumVitaeProjectBeforeUpsertHooks = append(curriculumVitaeProjectBeforeUpsertHooks, curriculumVitaeProjectHook)
	case boil.AfterUpsertHook:
		curriculumVitaeProjectAfterUpsertHooks = append(curriculumVitaeProjectAfterUpsertHooks, curriculumVitaeProjectHook)
	}
}

// One returns a single curriculumVitaeProject record from the query.
func (q curriculumVitaeProjectQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CurriculumVitaeProject, error) {
	o := &CurriculumVitaeProject{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for curriculum_vitae_projects")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all CurriculumVitaeProject records from the query.
func (q curriculumVitaeProjectQuery) All(ctx context.Context, exec boil.ContextExecutor) (CurriculumVitaeProjectSlice, error) {
	var o []*CurriculumVitaeProject

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to CurriculumVitaeProject slice")
	}

	if len(curriculumVitaeProjectAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all CurriculumVitaeProject records in the query.
func (q curriculumVitaeProjectQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count curriculum_vitae_projects rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q curriculumVitaeProjectQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if curriculum_vitae_projects exists")
	}

	return count > 0, nil
}

// CurriculumVitae pointed to by the foreign key.
func (o *CurriculumVitaeProject) CurriculumVitae(mods ...qm.QueryMod) curriculumVitaeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CurriculumVitaeID),
	}

	queryMods = append(queryMods, mods...)

	return CurriculumVitaes(queryMods...)
}

// Project pointed to by the foreign key.
func (o *CurriculumVitaeProject) Project(mods ...qm.QueryMod) projectQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ProjectID),
	}

	queryMods = append(queryMods, mods...)

	return Projects(queryMods...)
}

// LoadCurriculumVitae allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (curriculumVitaeProjectL) LoadCurriculumVitae(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCurriculumVitaeProject interface{}, mods queries.Applicator) error {
	var slice []*CurriculumVitaeProject
	var object *CurriculumVitaeProject

	if singular {
		var ok bool
		object, ok = maybeCurriculumVitaeProject.(*CurriculumVitaeProject)
		if !ok {
			object = new(CurriculumVitaeProject)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCurriculumVitaeProject)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCurriculumVitaeProject))
			}
		}
	} else {
		s, ok := maybeCurriculumVitaeProject.(*[]*CurriculumVitaeProject)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCurriculumVitaeProject)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCurriculumVitaeProject))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &curriculumVitaeProjectR{}
		}
		args = append(args, object.CurriculumVitaeID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &curriculumVitaeProjectR{}
			}

			for _, a := range args {
				if a == obj.CurriculumVitaeID {
					continue Outer
				}
			}

			args = append(args, obj.CurriculumVitaeID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`curriculum_vitae`),
		qm.WhereIn(`curriculum_vitae.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load CurriculumVitae")
	}

	var resultSlice []*CurriculumVitae
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice CurriculumVitae")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for curriculum_vitae")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for curriculum_vitae")
	}

	if len(curriculumVitaeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CurriculumVitae = foreign
		if foreign.R == nil {
			foreign.R = &curriculumVitaeR{}
		}
		foreign.R.CurriculumVitaeProjects = append(foreign.R.CurriculumVitaeProjects, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CurriculumVitaeID == foreign.ID {
				local.R.CurriculumVitae = foreign
				if foreign.R == nil {
					foreign.R = &curriculumVitaeR{}
				}
				foreign.R.CurriculumVitaeProjects = append(foreign.R.CurriculumVitaeProjects, local)
				break
			}
		}
	}

	return nil
}

// LoadProject allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (curriculumVitaeProjectL) LoadProject(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCurriculumVitaeProject interface{}, mods queries.Applicator) error {
	var slice []*CurriculumVitaeProject
	var object *CurriculumVitaeProject

	if singular {
		var ok bool
		object, ok = maybeCurriculumVitaeProject.(*CurriculumVitaeProject)
		if !ok {
			object = new(CurriculumVitaeProject)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCurriculumVitaeProject)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCurriculumVitaeProject))
			}
		}
	} else {
		s, ok := maybeCurriculumVitaeProject.(*[]*CurriculumVitaeProject)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCurriculumVitaeProject)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCurriculumVitaeProject))
			}
		}
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &curriculumVitaeProjectR{}
		}
		args = append(args, object.ProjectID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &curriculumVitaeProjectR{}
			}

			for _, a := range args {
				if a == obj.ProjectID {
					continue Outer
				}
			}

			args = append(args, obj.ProjectID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`projects`),
		qm.WhereIn(`projects.id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Project")
	}

	var resultSlice []*Project
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Project")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for projects")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for projects")
	}

	if len(projectAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Project = foreign
		if foreign.R == nil {
			foreign.R = &projectR{}
		}
		foreign.R.CurriculumVitaeProjects = append(foreign.R.CurriculumVitaeProjects, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ProjectID == foreign.ID {
				local.R.Project = foreign
				if foreign.R == nil {
					foreign.R = &projectR{}
				}
				foreign.R.CurriculumVitaeProjects = append(foreign.R.CurriculumVitaeProjects, local)
				break
			}
		}
	}

	return nil
}

// SetCurriculumVitae of the curriculumVitaeProject to the related item.
// Sets o.R.CurriculumVitae to related.
// Adds o to related.R.CurriculumVitaeProjects.
func (o *CurriculumVitaeProject) SetCurriculumVitae(ctx context.Context, exec boil.ContextExecutor, insert bool, related *CurriculumVitae) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"curriculum_vitae_projects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"curriculum_vitae_id"}),
		strmangle.WhereClause("\"", "\"", 2, curriculumVitaeProjectPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CurriculumVitaeID = related.ID
	if o.R == nil {
		o.R = &curriculumVitaeProjectR{
			CurriculumVitae: related,
		}
	} else {
		o.R.CurriculumVitae = related
	}

	if related.R == nil {
		related.R = &curriculumVitaeR{
			CurriculumVitaeProjects: CurriculumVitaeProjectSlice{o},
		}
	} else {
		related.R.CurriculumVitaeProjects = append(related.R.CurriculumVitaeProjects, o)
	}

	return nil
}

// SetProject of the curriculumVitaeProject to the related item.
// Sets o.R.Project to related.
// Adds o to related.R.CurriculumVitaeProjects.
func (o *CurriculumVitaeProject) SetProject(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Project) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"curriculum_vitae_projects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"project_id"}),
		strmangle.WhereClause("\"", "\"", 2, curriculumVitaeProjectPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ProjectID = related.ID
	if o.R == nil {
		o.R = &curriculumVitaeProjectR{
			Project: related,
		}
	} else {
		o.R.Project = related
	}

	if related.R == nil {
		related.R = &projectR{
			CurriculumVitaeProjects: CurriculumVitaeProjectSlice{o},
		}
	} else {
		related.R.CurriculumVitaeProjects = append(related.R.CurriculumVitaeProjects, o)
	}

	return nil
}

// CurriculumVitaeProjects retrieves all the records using an executor.
func CurriculumVitaeProjects(mods ...qm.QueryMod) curriculumVitaeProjectQuery {
	mods = append(mods, qm.From("\"curriculum_vitae_projects\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"curriculum_vitae_projects\".*"})
	}

	return curriculumVitaeProjectQuery{q}
}

// FindCurriculumVitaeProject retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCurriculumVitaeProject(ctx context.Context, exec boil.ContextExecutor, iD int, selectCols ...string) (*CurriculumVitaeProject, error) {
	curriculumVitaeProjectObj := &CurriculumVitaeProject{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"curriculum_vitae_projects\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, curriculumVitaeProjectObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from curriculum_vitae_projects")
	}

	if err = curriculumVitaeProjectObj.doAfterSelectHooks(ctx, exec); err != nil {
		return curriculumVitaeProjectObj, err
	}

	return curriculumVitaeProjectObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CurriculumVitaeProject) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no curriculum_vitae_projects provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(curriculumVitaeProjectColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	curriculumVitaeProjectInsertCacheMut.RLock()
	cache, cached := curriculumVitaeProjectInsertCache[key]
	curriculumVitaeProjectInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			curriculumVitaeProjectAllColumns,
			curriculumVitaeProjectColumnsWithDefault,
			curriculumVitaeProjectColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"curriculum_vitae_projects\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"curriculum_vitae_projects\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into curriculum_vitae_projects")
	}

	if !cached {
		curriculumVitaeProjectInsertCacheMut.Lock()
		curriculumVitaeProjectInsertCache[key] = cache
		curriculumVitaeProjectInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the CurriculumVitaeProject.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CurriculumVitaeProject) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	curriculumVitaeProjectUpdateCacheMut.RLock()
	cache, cached := curriculumVitaeProjectUpdateCache[key]
	curriculumVitaeProjectUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			curriculumVitaeProjectAllColumns,
			curriculumVitaeProjectPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update curriculum_vitae_projects, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"curriculum_vitae_projects\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, curriculumVitaeProjectPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, append(wl, curriculumVitaeProjectPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update curriculum_vitae_projects row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for curriculum_vitae_projects")
	}

	if !cached {
		curriculumVitaeProjectUpdateCacheMut.Lock()
		curriculumVitaeProjectUpdateCache[key] = cache
		curriculumVitaeProjectUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q curriculumVitaeProjectQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for curriculum_vitae_projects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for curriculum_vitae_projects")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CurriculumVitaeProjectSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), curriculumVitaeProjectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"curriculum_vitae_projects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, curriculumVitaeProjectPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in curriculumVitaeProject slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all curriculumVitaeProject")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CurriculumVitaeProject) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no curriculum_vitae_projects provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(curriculumVitaeProjectColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	curriculumVitaeProjectUpsertCacheMut.RLock()
	cache, cached := curriculumVitaeProjectUpsertCache[key]
	curriculumVitaeProjectUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			curriculumVitaeProjectAllColumns,
			curriculumVitaeProjectColumnsWithDefault,
			curriculumVitaeProjectColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			curriculumVitaeProjectAllColumns,
			curriculumVitaeProjectPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert curriculum_vitae_projects, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(curriculumVitaeProjectPrimaryKeyColumns))
			copy(conflict, curriculumVitaeProjectPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"curriculum_vitae_projects\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(curriculumVitaeProjectType, curriculumVitaeProjectMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert curriculum_vitae_projects")
	}

	if !cached {
		curriculumVitaeProjectUpsertCacheMut.Lock()
		curriculumVitaeProjectUpsertCache[key] = cache
		curriculumVitaeProjectUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single CurriculumVitaeProject record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CurriculumVitaeProject) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no CurriculumVitaeProject provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), curriculumVitaeProjectPrimaryKeyMapping)
	sql := "DELETE FROM \"curriculum_vitae_projects\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from curriculum_vitae_projects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for curriculum_vitae_projects")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q curriculumVitaeProjectQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no curriculumVitaeProjectQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from curriculum_vitae_projects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for curriculum_vitae_projects")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CurriculumVitaeProjectSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(curriculumVitaeProjectBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), curriculumVitaeProjectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"curriculum_vitae_projects\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, curriculumVitaeProjectPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from curriculumVitaeProject slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for curriculum_vitae_projects")
	}

	if len(curriculumVitaeProjectAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CurriculumVitaeProject) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCurriculumVitaeProject(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CurriculumVitaeProjectSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CurriculumVitaeProjectSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), curriculumVitaeProjectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"curriculum_vitae_projects\".* FROM \"curriculum_vitae_projects\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, curriculumVitaeProjectPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in CurriculumVitaeProjectSlice")
	}

	*o = slice

	return nil
}

// CurriculumVitaeProjectExists checks if the CurriculumVitaeProject row exists.
func CurriculumVitaeProjectExists(ctx context.Context, exec boil.ContextExecutor, iD int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"curriculum_vitae_projects\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if curriculum_vitae_projects exists")
	}

	return exists, nil
}

// Exists checks if the CurriculumVitaeProject row exists.
func (o *CurriculumVitaeProject) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CurriculumVitaeProjectExists(ctx, exec, o.ID)
}
